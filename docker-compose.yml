version: "3.9"
services:
  api:
    build:
      context: .
      target: deploy
    ports:
      - "8000:8000"
    volumes:
      - ./config.yaml:/app/config.yaml:ro
    depends_on:
      - redis
  redis:
    image: "redis:alpine"
